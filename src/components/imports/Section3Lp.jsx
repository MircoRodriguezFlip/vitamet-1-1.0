import { Form } from './form';

export const Section3LP = () => {
    return (
        <section className="section-container">
            <div>
                <Form />
            </div>
        </section>
    );
};
